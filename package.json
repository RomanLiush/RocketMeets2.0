{
  "scripts": {
    "prepare-client": "cd client && npm install && npm run build",
    "prepare-server": "cd server && npm install && npm start",
    "heroku-postbuild": "npm run prepare-client && npm run prepare-server",
    "start": "cd server && npm start"
  }
}
